import React from "react";
import {useDayPicker} from "../../actions/hook";
import Card from "../../atoms/cards/Card";
import moment from "moment";
import { IHouse } from "../../types/interface";

interface IProps {
    vacancy:,
    house: IHouse;
    domitoryCount: number;
    roomCount: number;
}

const DayVacancy: React.FC<IProps> = ({house, vacancy, roomCount, domitoryCount}) => {
  return (
    <div>
      
    </div>
  );
};

export default DayVacancy;
