type ChangePriorityResponse {
    ok: Boolean!
    error: String
    season: Season
}
type Mutation {
    ChangePriority(
        seasonId: ID!
        houseId: ID!
        priority: Int!
    ): ChangePriorityResponse!
}
