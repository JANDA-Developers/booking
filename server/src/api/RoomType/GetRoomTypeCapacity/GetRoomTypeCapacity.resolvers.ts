import { Resolvers } from "../../../types/resolvers";

const resolvers: Resolvers = {
    Query: {
        GetRoomTypeCapacity: () => {
            return;
        }
    }
};
export default resolvers;
