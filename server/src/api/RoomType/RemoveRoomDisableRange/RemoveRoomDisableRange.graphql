type RemoveRoomDisableRangeResponse {
    ok: Boolean!
    error: String
}
type Mutation {
    RemoveRoomDisableRange(
        roomId: ID!
        startDate: DateTime
        endDate: DateTime
    ): RemoveRoomDisableRangeResponse!
}
