type TestFunctionResponse {
    ok: Boolean!
    error: String
}
type Mutation {
    TestFunction(roomTypeId: ID!, guestId: ID!): TestFunctionResponse!
}

input PaddingGenderCountInput {
    female: Int!
    male: Int!
}
