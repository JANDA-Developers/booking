type RemoveRoomTypeDisableRangeResponse {
    ok: Boolean!
    error: String
}
type Mutation {
    RemoveRoomTypeDisableRange(
        roomTypeId: ID!
        houseId: ID!
        disableRange: DisableRangeInput!
    ): RemoveRoomTypeDisableRangeResponse!
}
