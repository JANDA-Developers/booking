type GetallocatedBedListResponse {
    ok: Boolean!
    error: String
    allocatedBedList: [allocatedBed!]
}
type allocatedBed {
    room: Room!
    date: DateTime!
    guests: [Guest!]
}
type Query {
    GetallocatedBedList(
        houseId: ID!
        start: DateTime!
        end: DateTime!
    ): GetallocatedBedListResponse!
}
