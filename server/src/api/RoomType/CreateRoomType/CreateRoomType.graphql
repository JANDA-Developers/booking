type CreateRoomTypeResponse {
    ok: Boolean!
    error: String
    roomType: RoomType
}

type Mutation {
    # 로그인 token 필요함!
    CreateRoomType(
        name: String!
        houseId: ID!
        img: URL
        pricingType: PricingType!
        peopleCount: Int!
        peopleCountMax: Int
        roomGender: RoomGender
        description: String
        tags: [TagInput!]
    ): CreateRoomTypeResponse!
}
