type BuyProductResponse {
    ok: Boolean!
    error: String
    product: Product
}

type Mutation {
    BuyProduct(
        houseId: ID!
        productTypeId: ID!
        layoutType: LayoutType
    ): BuyProductResponse!
}
