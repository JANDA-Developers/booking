type BuyProductResponse {
    ok: Boolean!
    error: String
    product: Product
}

type Mutation {
    BuyProduct(
        houseId: ID!
        productTypeId: ID!
        params: BuyProductParams!
    ): BuyProductResponse!
}

input BuyProductParams {
    layoutType: LayoutType
    appliedUrl: String
}
