type CreateRoomPriceResponse {
    ok: Boolean!
    error: String
    roomPrice: RoomPrice
}
type Mutation {
    CreateRoomPrice(
        price: Float!
        roomTypeId: ID!
        date: DateTime!
    ): CreateRoomPriceResponse!
}