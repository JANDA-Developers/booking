type GetRoomPriceResponse {
    ok: Boolean!
    error: String
    roomPrices: [RoomPrice!]
}
type Query {
    GetRoomPrice(
        roomTypeId: ID!
        start: DateTime!
        end: DateTime!
    ): GetRoomPriceResponse!
}
