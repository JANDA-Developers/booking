type HostApplication {
    _id: ID!
    applicationType: ApplicationType!
    house: House!
    user: User!
    url: URL!
    id: String!
    password: String
    description: String
    createdAt: DateTime!
    updatedAt: DateTime
}

enum ApplicationType {
    BOOKING_WEB
}
