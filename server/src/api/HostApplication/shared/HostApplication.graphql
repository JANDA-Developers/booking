type HostApplication {
    _id: ID!
    applicationType: ApplicationType!
    house: House!
    user: User!
    url: URL!
    layoutType: LayoutType!
    description: String
    createdAt: DateTime!
    updatedAt: DateTime
}

enum ApplicationType {
    BOOKING_WEB
}
