type RegisterSenderResponse {
    ok: Boolean!
    error: String
    sender: SmsSender
    verified: Boolean
}

type Mutation {
    RegisterSender(
        houseId: ID!
        sender: SmsSenderInput!
    ): RegisterSenderResponse!
}
