type SmsHistory {
    _id: ID!
    smsInfo: SmsInfo!
    msg: String!
    sender: String!
    receivers: [String!]
    sendResult: Boolean!
    autoSend: Boolean!
    createdAt: DateTime!
    updatedAt: DateTime!
}
