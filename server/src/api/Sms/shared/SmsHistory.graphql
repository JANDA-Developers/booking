type SmsHistory {
    _id: ID!
    smsInfo: SmsInfo!
    msg: String!
    sender: String!
    receivers: [String!]
    sendResult: Boolean!
    autoSend: Boolean!
    msgType: MsgType!
    createdAt: DateTime!
    updatedAt: DateTime!
}

enum MsgType {
    SMS
    LMS
    MMS
}
