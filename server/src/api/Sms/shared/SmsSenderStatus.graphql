type SmsSenderStatus {
    _id: ID!
    phoneNumber: PhoneNumber!
    registered: Boolean!
    registeredDate: DateTime
    user: User!
    createdAt: DateTime!
    updatedAt: DateTime
}

input SmsSenderStatusInput {
    phoneNumber: PhoneNumber!
    user: ID!
}
