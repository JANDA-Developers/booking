type GetHousesForSUResponse {
    ok: Boolean!
    error: String
    houses: [House!]
    pageInfo: PageInfoOffsetBase
}
type Query {
    # 슈퍼계정으로 모든 집들을 가져옴.
    GetHousesForSU(
        # 오프셋...
        page: Int!
        # 출력 개수
        count: Int!
    ): GetHousesForSUResponse!
}
