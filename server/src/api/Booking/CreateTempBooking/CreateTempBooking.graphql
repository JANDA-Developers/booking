type CreateTempBookingResponse {
    ok: Boolean!
    error: String
}

type Mutation {
    # Host가 예약을 생성할 떄...
    CreateTempBooking(
        tempBookingInputs: [TempBookingInput!]
    ): CreateTempBookingResponse!
}

input TempBookingInput {
    roomId: ID!
    gender: Gender!
}
