type UpdateBookingResponse {
    ok: Boolean!
    error: String
    bookings: [Booking!]
}
type Mutation {
    UpdateBooking(
        bookingId: ID!
        bookingStatus: BookingStatus
        start: DateTime
        end: DateTime
        price: Float
    ): UpdateBookingResponse!
}
