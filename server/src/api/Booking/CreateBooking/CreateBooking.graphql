type CreateBookingResponse {
    ok: Boolean!
    error: String
    # 리턴타입 검토중. 
    booking: [Booking!]
}
type Mutation {
    CreateBooking(
        bookingParams: BookingInput!
    ): CreateBookingResponse!
}
