type Verification {
    _id: ID!
    target: VerificationTarget!
    payload: String!
    verified: Boolean!
    key: String!
    user: User!
    house: House
    createdAt: String!
    updatedAt: String
}

enum VerificationTarget {
    PHONE
    EMAIL
}
