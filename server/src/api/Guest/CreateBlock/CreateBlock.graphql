type CreateBlockResponse {
    ok: Boolean!
    error: String
    guest: Guest
}
type Mutation {
    CreateBlock(
        start: DateTime!
        end: DateTime!
        houseId: ID!
        roomId: ID!
        bedIndex: Int!
    ): CreateBlockResponse!
    DeleteBlock(blockId: ID!): Response!
}
