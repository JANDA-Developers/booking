type AllocateGuestToRoomResponse {
    ok: Boolean!
    error: String
    guest: Guest
}

type Mutation {
    # 배정확정인 Guest는 옮길 수 없음.
    AllocateGuestToRoom(
        roomId: ID!
        guestId: ID!
        bedIndex: Int!
    ): AllocateGuestToRoomResponse!
}
