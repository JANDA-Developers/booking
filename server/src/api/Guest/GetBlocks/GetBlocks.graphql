type GetBlocksResponse {
    ok: Boolean!
    error: String
    blocks: [Block!]
}
type Query {
    GetBlocks(
        start: DateTime!
        end: DateTime!
        houseId: ID!
    ): GetBlocksResponse!
}
