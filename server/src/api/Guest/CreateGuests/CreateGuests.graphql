type CreateGuestsResponse {
    ok: Boolean!
    error: String
    guests: [Guest!]
}
type Mutation {
    # 게스트 생성
    CreateGuests(
        houseId: ID!
        bookingId: ID!
        roomTypeId: ID!
        roomIds: [ID!]
        start: DateTime!
        end: DateTime!
        pricingType: PricingType!
    ): CreateGuestsResponse!
}
