type User {
    _id: ID!
    firstName: String!
    lastName: String!
    phoneNumber: String!
    password: String
    email: String!
    verifiedPhone: Boolean!
    verifiedEmail: Boolean!
    userRole: UserRole!
    checkPrivacyPolicy: Boolean!
    createdAt: DateTime!
    updatedAt: DateTime
}

enum UserRole {
    ADMIN
    HOST
    BOOKER
    GHOST
}